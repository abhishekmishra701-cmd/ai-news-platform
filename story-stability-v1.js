(()=>{'use strict';
if(window.__GN_STORY_STABILITY_V1__)return;window.__GN_STORY_STABILITY_V1__=true;
const desc=Object.getOwnPropertyDescriptor(Element.prototype,'innerHTML');
if(desc&&desc.set&&desc.get){Object.defineProperty(Element.prototype,'innerHTML',{configurable:desc.configurable,enumerable:desc.enumerable,get:desc.get,set:function(v){if(this instanceof Element&&(this.matches('.ai-sidebar')||this.matches('.ai-langs'))&&desc.get.call(this)===String(v))return;return desc.set.call(this,v)}})}
const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
const esc=v=>clean(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const findStory=()=>{const h=document.querySelector('#article .story-reader-head h1');const title=clean(h?.dataset?.gnFinalOriginal||h?.textContent);if(!title)return null;const pools=[window.__GLOBAL_NEWS_API_STORIES__,window.__GLOBAL_NEWS_STORIES__,window.stories];for(const p of pools){if(Array.isArray(p)){const s=p.find(x=>clean(x?.headline||x?.title).toLowerCase()===title.toLowerCase());if(s)return s}}return null};
const split=v=>clean(v).split(/(?<=[.!?])\s+(?=[A-Z0-9“"'])|\n+/).map(clean).filter(x=>x.length>35);
function fallback(){const d=document.querySelector('#detail');if(!d||d.classList.contains('hidden'))return;const b=document.querySelector('#storyReaderBrief'),r=document.querySelector('#storyReaderReport'),s=findStory();if(!s||!b||!r)return;const briefLoading=/Loading/i.test(b.textContent||''),reportLoading=/Loading/i.test(r.textContent||'');if(!briefLoading&&!reportLoading)return;const summary=clean(s.summary||s.description),body=clean(s.body),pts=split(body).filter(x=>x.toLowerCase()!==clean(s.headline||s.title).toLowerCase()).slice(0,12);if(briefLoading){const bp=pts.length?pts.slice(0,3):split(summary).slice(0,3);b.innerHTML=bp.length?'<ul>'+bp.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ul>':'<div class="story-reader-limited"><strong>Story Brief is limited.</strong><br>No additional source-grounded detail is available yet.</div>'}if(reportLoading&&pts.length>=4){const rp=pts.slice(3);r.innerHTML='<div class="story-reader-label">Available source detail</div>'+rp.map(x=>'<p>'+esc(x)+'</p>').join('');r.dataset.reportStatus='limited';return}if(reportLoading){r.innerHTML='<div class="story-reader-limited"><strong>Detailed source report is temporarily unavailable.</strong><br>The attributed publisher did not return enough source text for a reliable full report. No unsupported details are invented.</div>';r.dataset.reportStatus='limited'}}
function arm(){setTimeout(fallback,12000)}
new MutationObserver(()=>{const d=document.querySelector('#detail');if(d&&!d.classList.contains('hidden')&&!window.__GN_STORY_STABILITY_TIMER__){window.__GN_STORY_STABILITY_TIMER__=true;arm()}}).observe(document.body,{childList:true,subtree:true});
})();

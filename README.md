# AI News Platform Web V2
Production static frontend for Supabase-backed AI News.
V2 fixes the misleading empty "No published stories found" state when the Home page has exactly one featured story.
Uses only a Supabase publishable key in the browser; never use a service-role key here.
V2 deployment trigger

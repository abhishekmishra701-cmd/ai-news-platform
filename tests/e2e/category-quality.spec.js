const { test, expect } = require('@playwright/test');
const cases=[['Technology',/tech|ai|chip|software|semiconductor/i],['Entertainment',/film|movie|music|actor|streaming|entertainment/i],['Geopolitics',/geopolit|war|sanction|border|military|defence|defense/i],['International Relations',/diplomatic|diplomacy|summit|foreign|ambassador|treaty|relations/i],['Business',/business|company|market|stock|economy|finance|investment|trade/i],['Sports',/sport|football|soccer|cricket|tennis|basketball|olympic|match/i],['Science',/science|scientist|research|space|nasa|physics|biology|discovery/i],['Climate',/climate|warming|carbon|emission|renewable|drought|flood|wildfire|cyclone/i]];
test.describe('category quality',()=>{for(const [category,signal] of cases){test(`${category} stories are category-relevant and attributed`,async({page})=>{await page.goto('/');const tab=page.locator('[data-cat="'+category+'"]').click();await tab;await expect(page.locator('#listTitle')).toContainText(category);const cards=page.locator('#grid .card');await expect(cards.first()).toBeVisible();const count=await cards.count();expect(count).toBeGreaterThan(0);let relevant=0;for(let i=0;i<count;i++){const card=cards.nth(i),text=await card.innerText();if(signal.test(text))relevant++;await expect(card.locator('.chip').last()).toBeVisible()}expect(relevant).toBeGreaterThan(0);});}});
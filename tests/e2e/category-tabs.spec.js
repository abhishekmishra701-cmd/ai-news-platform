const { test, expect } = require('@playwright/test');
const categories=['India','World','Geopolitics','International Relations','Business','Technology','Entertainment','Sports','Science','Climate'];
test.describe('category tabs',()=>{for(const category of categories){test(`${category} tab loads stories`,async({page})=>{await page.goto('/');const tab=page.locator('[data-cat="'+category+'"]');await expect(tab).toBeVisible();await tab.click();await expect(tab).toHaveClass(/active/);await expect(page.locator('#listTitle')).toContainText(category);await expect(page.locator('#count')).not.toHaveText('0 stories');await expect(page.locator('#grid .card').first()).toBeVisible();await expect(page.locator('#grid .card .chip').last()).toBeVisible();});}});
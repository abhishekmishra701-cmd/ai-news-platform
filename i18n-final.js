(()=>{'use strict';const KEY='globalNewsLanguage',CACHE='gn-final-i18n-v2:',RTL=new Set(['ar','ur']);const SUPPORTED=new Set(['en','hi','zh','es','ar','fr','bn','pt','ru','ur','id']);const lang=()=>{const v=localStorage.getItem(KEY)||'en';return SUPPORTED.has(v)?v:'en'};const clean=v=>String(v??'').replace(/\s+/g,' ').trim();const isEnglish=v=>/[A-Za-z]{3}/.test(v||'');const bad=v=>/QUERY LENGTH LIMIT EXCEEDED|MAX ALLOWED QUERY|TRANSLATION FAILED|HTTP\s*4\d\d/i.test(v||'');function theme(){if(document.getElementById('gn-final-theme'))return;const s=document.createElement('style');s.id='gn-final-theme';s.textContent=`.gn-i18n-pending{opacity:.65}.gn-i18n-pending *{visibility:hidden}.gn-i18n-pending:after{content:'Translating…';visibility:visible;color:#667085;font-size:13px}.story-reader-error,.notice.error{display:none!important}html[dir=rtl] body{direction:rtl;text-align:right}`;document.head.appendChild(s)}function split(text,max=1000){const out=[];let s=clean(text);while(s.length>max){let i=Math.max(s.lastIndexOf('. ',max),s.lastIndexOf('? ',max),s.lastIndexOf('! ',max),s.lastIndexOf(', ',max));if(i<300)i=max;out.push(s.slice(0,i+1));s=s.slice(i+1).trim()}if(s)out.push(s);return out}async function req(text,to){const k=CACHE+to+':'+text;try{const hit=localStorage.getItem(k);if(hit)return hit}catch(_){}let v='';try{const r=await fetch('/api/translate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,to})});if(r.ok){const j=await r.json();v=clean(j?.text)}}catch(_){}if(!v||bad(v)||v===text){const u='https://api.mymemory.translated.net/get?q='+encodeURIComponent(text)+'&langpair=en|'+encodeURIComponent(to);const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw Error('translation unavailable');const j=await r.json();v=clean(j?.responseData?.translatedText)}if(!v||bad(v)||v===text)throw Error('translation unavailable');try{localStorage.setItem(k,v)}catch(_){}return v}async function translate(text,to){if(to==='en')return text;const parts=split(text);const out=[];for(const p of parts){try{out.push(await req(p,to))}catch(_){return null}}return out.join(' ')}const SELECTORS=['.hero h1','.hero p','.hero .read','.card h3','.card p','.card .read','.story-reader-head h1','.story-reader-head p','.story-reader-section h2','.story-reader-back','#storyReaderBrief li','#storyReaderReport p','#storyReaderReport .story-reader-label','#storyReaderReport .story-reader-note'];function originals(){const out=[];for(const sel of SELECTORS)document.querySelectorAll(sel).forEach(el=>{if(!el.isConnected)return;if(bad(el.textContent)){el.textContent='';return}if(isEnglish(el.textContent))out.push(el)});return [...new Set(out)]}let busy=false,timer=0,last='';async function apply(){if(busy)return;busy=true;try{const to=lang();document.documentElement.lang=to;document.documentElement.dir=RTL.has(to)?'rtl':'ltr';if(to==='en')return;const nodes=originals();for(let i=0;i<nodes.length;i+=3){if(lang()!==to)return;await Promise.all(nodes.slice(i,i+3).map(async el=>{if(!el.dataset.gnFinalOriginal)el.dataset.gnFinalOriginal=clean(el.textContent);const src=el.dataset.gnFinalOriginal;if(!src||!isEnglish(src))return;el.classList.add('gn-i18n-pending');const v=await translate(src,to);if(v&&lang()===to)el.textContent=v;el.classList.remove('gn-i18n-pending')}))}}finally{busy=false}}function schedule(ms=350){clearTimeout(timer);timer=setTimeout(apply,ms)}function start(){theme();last=lang();schedule(50);new MutationObserver(ms=>{if(ms.some(m=>m.type==='childList'&&m.addedNodes.length))schedule(450)}).observe(document.body,{childList:true,subtree:true});setInterval(()=>{const now=lang();if(now!==last){last=now;schedule(50)}document.querySelectorAll('*').forEach(el=>{if(el.children.length===0&&bad(el.textContent))el.textContent=''})},250)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();})();
